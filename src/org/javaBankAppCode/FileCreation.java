package org.javaBankAppCode;

public class FileCreation {
	public static void main(String[] args) {
				
				//Create directory
				//File file = new File("WebContent/resourceconfig");
				//file.mkdirs();
				
				//adding data to the properties file
				/*Properties prop = new Properties();
				prop.setProperty("adminemail", "royalbankofindia.alert@gmail.com");
				prop.setProperty("password", "*********");
				
				try{
					//setting the file location
					File file = new File("WebContent/resourceconfig/config.properties");
					FileOutputStream fout = new FileOutputStream(file);
					//storing the file
					prop.store(fout, "Setting the email information");
					fout.close();
				}catch(IOException e){
					e.printStackTrace();
				}*/
		
		//Accessing the file and its properties
				
	}
}
